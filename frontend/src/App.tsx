// Головний компонент (крок 1 — лише перевірка, що Vite + React працюють)

function App() {
  return (
    <main className="app">
      <h1>Status Check</h1>
      <p>Frontend is running (Vite + React + TypeScript).</p>
      <p className="hint">Step 2: connect to FastAPI backend.</p>
    </main>
  );
}

export default App;
